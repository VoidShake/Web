import { Theme } from "@emotion/react";

const theme: Theme = {
   bg: '#dbd8d0',
   text: '#000',
   primary: '#e6be50',
   secondary: '#858072',
   tooltip: '#858072',
   darker: 0.1,
}

export default theme